import type React from "react"
import type { Metadata } from "next"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

export const metadata: Metadata = {
  title: "Image to Sketch - Transform Photos into Beautiful Sketches with AI",
  description:
    "Turn any image into stunning sketch art in seconds. Create professional pencil drawings, charcoal sketches, cartoon styles, and coloring book pages.",
  generator: "v0.app",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className="font-sans antialiased">
        {children}
        <Analytics />
      </body>
    </html>
  )
}
